import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.farasi.asms',
  appName: 'ASMS',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
